# Career Pilot AI frontend environment
# Copy to .env.local for local development.

NEXT_PUBLIC_API_URL=http://localhost:5000/api/v1
# Optional. Docker builds set this to "standalone".
NEXT_OUTPUT_MODE=
